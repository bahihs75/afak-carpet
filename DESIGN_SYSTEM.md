# AFAK CARPET — Design System

## الغرض

هذا الملف هو المرجع العملي لنظام تصميم AFAK CARPET في الكود. يجب أن يشرح أي تعديل بصري جديد هل يعيد استخدام Token أو مكونًا موجودًا، وما الحالات التي يغطيها، وهل يعمل بالعربية والإنجليزية وعلى الهاتف والكمبيوتر.

## الملفات الأساسية

| الملف | المسؤولية |
|---|---|
| `design-tokens.css` | الألوان، الخطوط، الحركة، المسافات، الزوايا، الظلال، الطبقات، ونسب الصور. |
| `style.css` | المكونات العامة والواجهة العامة ولوحة الإدارة، ويعتمد على Tokens. |
| `index.html` | بنية الموقع العامة والـHero والأقسام والمنتجات والتواصل. |
| `admin.html` | بنية لوحة الإدارة والصلاحيات والنماذج والجداول. |
| `404.html` | صفحة الخطأ، وتحمّل Tokens قبل CSS المشترك. |

تُحمّل `design-tokens.css` قبل `style.css` في الصفحات الثلاث. لا تضع قيمًا بديلة متعارضة داخل مكون واحد إلا إذا كانت حالة مخصصة ومبررة.

## هوية العلامة

الهوية هادئة ومؤسسية وتحريرية. يعتمد النظام على الكحلي كعنصر تمييز وحيد، وخلفيات بيضاء ومحايدة، وتصوير كبير، وحدود رفيعة، ومساحات واضحة، وزوايا حادة في معظم المكونات. لا تُستخدم الزخرفة أو الألوان الصارخة للتعويض عن ضعف المحتوى.

## Tokens

### الألوان

| Token | القيمة | الاستخدام |
|---|---|---|
| `--navy` | `#17233F` | اللون الأساسي والأزرار النشطة. |
| `--navy-active` | `#0E1830` | الضغط على الزر الأساسي. |
| `--navy-hover` | `#223060` | المرور فوق الزر الأساسي. |
| `--ink` | `#14171F` | العناوين، الخلفيات الداكنة، والتذييل. |
| `--body` | `#4B4F58` | نصوص الفقرات. |
| `--muted` | `#7C808A` | النصوص الثانوية والـPlaceholder. |
| `--hairline` | `#E2E2DE` | الحدود والفواصل الأساسية. |
| `--hairline-soft` | `#EFEFEC` | الفواصل الخفيفة. |
| `--canvas` | `#FFFFFF` | الخلفية الرئيسية والبطاقات. |
| `--canvas-soft` | `#F5F5F3` | الأقسام المحايدة ولوحة الإدارة. |
| `--canvas-strong` | `#ECECE8` | خلفيات الصور الفارغة. |
| `--success` | `#2E7D32` | النجاح فقط. |
| `--danger` | `#B3261E` | الخطأ والحذف والتحذير فقط. |

لا تضف لونًا جديدًا إلى CSS قبل التأكد من أن أحد هذه Tokens لا يغطي الحالة. ألوان المنتجات تُحفظ داخل بيانات المنتج أو عينة اللون، ولا تتحول إلى ألوان عامة للنظام.

### الخطوط

العناوين والنصوص تستخدم `AlJazeeraArabic` مع بدائل `Tajawal` و`Segoe UI`. التسميات التقنية والأرقام الإرشادية تستخدم `IBM Plex Mono`. لا تستخدم تباعدًا أفقيًا واسعًا للعربية؛ يمكن استعماله في الإنجليزية فقط عندما لا يقطع الحروف.

### المسافات

الأساس 4px، وتستخدم القيم `--sp-1` إلى `--sp-9` من 4px إلى128px. يجب أن تكون المسافات الجديدة من هذا السلم، خصوصًا 8px و16px و24px و32px و48px.

### الشكل والارتفاع

يستخدم النظام `--radius-none` للمكونات الأساسية، و`--radius-soft` للحالات الصغيرة الوظيفية، و`--radius-circle` للنقاط والأسهم وبعض عناصر التحكم. الظلال المركزية هي `--shadow-header` و`--shadow-card` و`--shadow-button` و`--shadow-popover` و`--shadow-consent` و`--shadow-drawer` و`--shadow-admin-drawer`.

### الطبقات

تستخدم العناصر Tokens الطبقات بدل أرقام جديدة عشوائية: المحتوى، صور المنتجات، شريط التقدم، الهيدر، الـDrawer، الأدوات العائمة، التفاصيل، النوافذ، رأس الإدارة، Toast، Banner الموافقة، Loader، وSkip Link. عند إضافة طبقة جديدة، ضعها في `design-tokens.css` وحدد سببها.

### الحركة

القيم الأساسية هي `--dur-fast: 120ms` و`--dur: 220ms` و`--dur-slow: 420ms`. الحركة وظيفية وخفيفة، وتستخدم `--ease` أو `--ease-emphasized`. يجب دائمًا دعم `prefers-reduced-motion: reduce`، وألا تعتمد الوظيفة على الحركة وحدها.

## نسب الصور

### قاعدة الكتالوج الموحدة

**كل صور بطاقات المنتجات في جميع الأقسام تستخدم نسبة 4:3.** هذا يشمل المساجد والفنادق والروضات والقاعات، سواء كان Slider أو Grid. مصدر النسبة هو `--ratio-product: 4 / 3`.

| السياق | Token | النسبة |
|---|---|---|
| بطاقة المنتج وSlider المنتجات | `--ratio-product` | 4:3 |
| بطاقة التصنيف | `--ratio-category` | 3:4 |
| صورة من نحن | `--ratio-about` | 4:3 |
| بلاطة المشروع | `--ratio-project` | 4:3 |
| صورة Hero في التفاصيل | `--ratio-detail` | 16:10 |
| معرض تفاصيل المنتج | `--ratio-gallery` | 4:3 |
| مكتبة الوسائط | `--ratio-square` | 1:1 |

نسبة 3:4 مخصصة لبطاقات التصنيف، وليست لصور بطاقات المنتجات. لا تعِد استخدام `padding-top:133.33%` لبطاقة منتج.

`object-fit: cover` مناسب للصور التي يجب أن تملأ الإطار، و`object-fit: contain` مناسب للصور التي يجب ألا تُقص، مثل بعض صور التفاصيل والشهادات.

## المكونات الأساسية

### Header

الهيدر ثابت فوق Hero، شفاف في البداية، ثم يتحول إلى خلفية بيضاء شبه شفافة مع Blur وخط سفلي عند التمرير. على الهاتف تختفي روابط سطح المكتب ويظهر Drawer.

### Hero

Hero كامل الشاشة ويستخدم صورة كبيرة وتدرجًا لضمان التباين. يمكن لكل شريحة استخدام صورة سطح مكتب وصورة هاتف، وخيار Reflection، ولون نص مخصص، وظل نصي خلف المحتوى. لا تستخدم صندوقًا زجاجيًا كبيرًا خلف العنوان بدل التدرج أو ظل النص.

### Button

الزر الأساسي كحلي بارتفاع48px، والنص أبيض، والزوايا حادة. الزر المخطط ثانوي. زر طلب عرض السعر هو الإجراء الأهم ويجب ألا يختفي خلف زخارف أو أزرار كثيرة.

### Product Card

البطاقة تتكون من صورة 4:3، ثم صف الاسم والسعر، ثم زر طلب عرض السعر عند وجوده. الصور الإضافية قد تعمل عبر Slider أو Hover حسب الإعدادات. على الأجهزة التي لا تملك مؤشرًا لا تعتمد على Hover.

### Color Filter

عينات الألوان مربعة. اللون المحدد يُبرز بحد أسود واضح، ويجب إزالة الاختيار القديم مباشرة عند اختيار لون جديد.

### Detail Overlay

تفاصيل المنتج تظهر في Overlay كامل الشاشة ينزلق من الجانب، وليس في صندوق يتبع المؤشر. صورة البطل في التفاصيل 16:10، ومعرض الصور 4:3.

### Admin Sidebar

على الكمبيوتر Sidebar ثابتة في اليمين بعرض260px تقريبًا. على الهاتف تصبح Drawer منزلقًا مع Scrim. العنصر النشط كحلي، والحذف باللون الأحمر فقط.

## الحالات المطلوبة لكل مكون

قبل اعتماد أي مكون يجب اختبار الحالات التالية:

| الحالة | المطلوب |
|---|---|
| Default | يظهر بالمقاسات والألوان الأساسية. |
| Hover | تغيير بصري صغير على الأجهزة ذات المؤشر فقط. |
| Focus | حلقة تركيز واضحة للوحة المفاتيح. |
| Active | استجابة ضغط قصيرة، دون تحريك التخطيط. |
| Disabled | شفافية أقل ومؤشر غير متاح. |
| Loading | حالة مفهومة لا تترك فراغًا غامضًا. |
| Empty | رسالة واضحة عند غياب المنتجات أو المشاريع. |
| Error | رسالة داخل السياق بلون الخطأ، دون إخفاء السبب. |
| No image | خلفية Placeholder من `--canvas-strong`. |
| Reduced motion | إيقاف الحركة غير الضرورية. |
| Touch | لا تعتمد على Hover أو مؤشر الفأرة. |

## الاستجابة

اختبر العرض عند 360px، 480px، 560px، 640px، 900px، 1024px، والشاشات العريضة. عند 900px تتحول الروابط العامة إلى قائمة، وعند 560px تتحول شبكات المنتجات إلى عمود واحد مع خيارات الهاتف المناسبة. لا تسمح للنصوص العربية أو الشعار بالتزاحم أو الخروج من الهيدر.

## الوصولية

يجب الحفاظ على Skip Link، `focus-visible`، تسميات الحقول، `aria-label` لعناصر التحكم التي لا تحمل نصًا، مناطق لمس تقارب44–48px، نصوص بديلة للصور، وحالات لا تعتمد على اللون وحده. يجب أن يعمل التنقل والـDrawer والنوافذ بلوحة المفاتيح.

## الأداء

صور Hero لا تستخدم Lazy لأنها أول محتوى مرئي. الصور تحت الطية تستخدم Lazy عند ملاءمة ذلك. تجنب تحميل صور غير ظاهرة أو تكرار تحميل نفس الصورة في بطاقة لا تحتاجها. لا تضف حركة تؤثر على Layout أو تغيّر أبعاد الشبكة أثناء التفاعل.

## حوكمة التعديلات

قبل أي تعديل بصري، يجب أولًا البحث عن Token أو مكون قائم. إذا كانت القيمة مشتركة، أضفها إلى `design-tokens.css` بدل تكرارها في `style.css`. إذا كانت النسبة تخص سياقًا واحدًا، استخدم Token مناسبًا بدل تعديل `--ratio-product`.

أي تغيير في الألوان أو الخطوط أو موضع الشعار يحتاج قرارًا منفصلًا، لأن هذه العناصر جزء من هوية العلامة. أما إنشاء Figma أو Storybook فهو مرحلة توثيق وتصميم مستقبلية، وليس شرطًا لتشغيل الموقع أو توحيد CSS الحالي.

## سجل الفجوات بعد التحديث

| الفجوة | الحالة |
|---|---|
| نسب صور المنتجات غير موحدة | تم إصلاحها: كل بطاقات المنتجات 4:3. |
| Tokens مركزية غير منفصلة | تم إصلاحها بإضافة `design-tokens.css`. |
| ظلال وz-index وقيم الشكل مكررة | تم توحيد القيم المشتركة في Tokens واستخدامها في CSS المشترك. |
| توثيق المكونات والحالات | تم توثيقه في هذا الملف. |
| مكتبة مكونات مرئية أو Storybook | خارج نطاق التحديث الحالي؛ يمكن بناؤها لاحقًا. |
| ملف Figma مرتبط بالكود | خارج نطاق التحديث الحالي؛ يحتاج عملية تصميم مستقلة. |

## قاعدة المراجعة

إذا كان التعديل لا يزيد الوضوح أو يحل مشكلة استخدام حقيقية، فلا يُضاف. يجب أن يبقى الموقع هادئًا، مؤسسيًا، عربيًا أولًا، ومتسقًا في الهاتف والكمبيوتر.
